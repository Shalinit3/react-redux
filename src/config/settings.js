export const scrollToViewSettings = Object.freeze({
  block: 'end',
  behaviour: 'smooth'
})

export const chartColors = Object.freeze({
  PRIMARY: '#009c95',
  SELECTED: '#f2711c',
  TEXT: '#333333'
})

export const pieConfig = Object.freeze({
  LABEL_FONTSIZE: '8',
  LABEL_TRUNCATE: '8'
})

export const barConfig = Object.freeze({
  LABEL_FONTSIZE: '10',
  LABEL_TRUNCATE: '8'
})

export const mathConfig = Object.freeze({
  RADIAN: Math.PI / 180
})
